%h2
  Pictures
%table.table.table-striped
  %thead
    %tr
      %th
        image
  %tbody
    - @pictures.each do |picture|
      %tr
        %td
          = link_to picture.image.url, picture
= link_to 'create picture', new_picture_path, class: 'btn btn-primary'