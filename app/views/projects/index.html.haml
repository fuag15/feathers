- content_for :browser do
  \/dev/boone/projects
- content_for :title do
  Projects
  = render partial: 'shared/authentication'
.main-banner= image_tag 'DavisDeathStar.jpg'
- if current_user.try :admin?
  %p.clearfix= link_to 'manage projects', project_categories_path, class: 'btn pull-right'
= render partial: 'shared/category', collection: @job_categories
.text-center= paginate @project_categories, theme: 'twitter-bootstrap'