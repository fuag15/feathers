!!!
%html
  %head
    %title
      = yield :browser
    = render partial: 'layouts/meta'
    = stylesheet_link_tag "application", media: "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    #Center{ align: "center" }
      #Holder
        #topMainBorder
        #dropShadowHolder
          #leftMainBorder
          #BackGround
            #Text
              = render 'layouts/flash_messages'
              %center
                #mainTitle
                  = yield :title
              %br
              %br
              = yield
          #rightMainBorder
          #clear
        #botMainBorder
      = render partial: "layouts/navigation"
      #clear
      .Sig
        %i
          C #{Time.now.year.to_s} Michael Boone