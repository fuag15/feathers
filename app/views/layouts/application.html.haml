!!!
%html
  %head
    = render partial: 'layouts/meta'
    %title= yield :browser
  %body
    .main-container
      .output-group.clearfix
        .output-container
          .main-output
            = render 'layouts/flash_messages'
            %p.main-title.text-center= yield :title
            = render partial: 'layouts/authentication'
            = yield
        = render partial: 'layouts/navigation'
      %em.signature C #{Time.now.year.to_s} Michael Boone