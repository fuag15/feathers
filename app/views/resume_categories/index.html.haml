%h2
  Resume Categories
%table.table.table-striped
  %thead
    %tr
      %th
        name
      %th
        rank
  %tbody
    - @resume_categories.each do |resume_category|
      %tr
        %td
          = link_to resume_category.name, resume_category
        %td
          = resume_category.rank
= link_to 'create resume_category', new_resume_category_path, class: 'btn btn-primary'