= form_for [project, package] do |f|
  = render partial: 'shared/form_error', collection: package.errors.full_messages
  = f.file_field :file
  = f.text_field :version, placeholder: 'version'
  = f.submit class: 'btn btn-primary'